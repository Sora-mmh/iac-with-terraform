terraform {
  required_providers {
    aws = {
      source = "hashicorp/aws"
      version = "6.0.0-beta3"
    }
    # linode = {
    #   source = "linode/linode"
    #   version = "2.41.0"
    # }
  }
}
 
