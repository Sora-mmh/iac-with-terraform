terraform {
  required_providers {
    aws = {
      source = "hashicorp/aws"
      version = "5.20.1"
    }
    # linode = {
    #   source = "linode/linode"
    #   version = "2.41.0"
    # }
  }
}
 
